source 'http://rubygems.org'
ruby '2.3.0'

# utils:
gem 'rubocop' # take this to dev only, for Atom linting
gem 'pry' # take this to dev only, for console.rb
gem 'colorize'
gem 'json'

# server:
gem 'pg'
gem 'activerecord', '5.0.0.beta2'
gem 'activesupport', '5.0.0.beta2'
gem 'roar', '~> 1.0', '>= 1.0.4'
gem 'webmachine'

# client:
gem 'httparty'

group :test do
  gem 'rspec'
  gem 'webmachine-test', git: 'https://github.com/bernd/webmachine-test.git'
end
